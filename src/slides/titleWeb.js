import React from 'react'
import {
    List,
    ListItem,
    Appear,
    Slide,
    Heading,
} from 'spectacle'

const slide = (
    <Slide bgColor="background" maxWidth={100} maxHeight={100}>
        <Heading style={{ marginTop: '-10rem' }} fit size={1} textColor="primary" lineHeight={2}>L&apos;application web</Heading>
    </Slide>
)

export default slide